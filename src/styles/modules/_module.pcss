.container {
    @media (width >= 1200px) {
        width: 1200px;
    }
}

.jumbotron {
    color: var(--colorWhite);
    background: transparent;
    padding-top: 135px;
    margin-bottom: 110px;

    &__preheader {
        font-family: var(--fontKaushan);
        font-size: 49px !important;
        line-height: 50px;

        @media (width >= 992px) {
            font-size: 72px !important;
            line-height: 50px;
        }
    }

    &__header {
        font-family: var(--fontMontserrat);
        font-size: 49px !important;
        line-height: 0.9em;
        text-transform: uppercase;
        font-weight: bold;
        margin-bottom: 0;

        @media (width >= 768px) {
            font-size: 110px !important;
        }

        @media (width >= 992px) {
            font-size: 150px !important;
        }
    }

    &__divider {
        display: block;
        margin: 70px auto 45px;
        width: 60px;
        height: 4px;
        background: var(--colorWhite);
    }

    &__button {
        display: block;
        margin: 0 auto;
        max-width: 160px;
        border: 2px solid var(--colorWhite);
        font-size: 14px;
        font-family: var(--fontMontserrat);
        text-transform: uppercase;
        padding: 9px 0;
        color: var(--colorWhite);
        transition: all 0.5s;

        &:hover {
            color: var(--colorWhite);
            background: color(var(--colorWhite) a(20%));
            text-decoration: none;
        }
    }

    @media (width < 768px) {
        padding-top: 50px;
    }
}

.scrollspy-block {
    &__btn {
        display: block;
        width: 100%;
        max-width: 300px;
        margin: 0 auto;
        border-top: 4px solid var(--colorWhite);
        color: var(--colorWhite);
        font-size: 18px;
        font-family: var(--fontMontserrat);
        text-decoration: none;
        padding: 24px 0;

        > span {
            font-size: 24px;
            font-weight: bold;
        }

        &:hover {
            text-decoration: none;
            color: var(--colorWhite);
        }
    }
}

.section-title {
    color: var(--colorDark);
    text-align: center;
    margin: 0 auto;

    &__preheader {
        font-family: var(--fontKaushan);
        font-size: 18px;
    }

    &__header {
        font-family: var(--fontMontserrat);
        font-size: 30px;
        font-weight: bold;
        margin: 20px 0 40px;
        text-transform: uppercase;
    }

    &__divider {
        background: var(--colorRed);
        display: inline-block;
        height: 3px;
        width: 60px;
    }

    &__text {
        font-family: var(--fontRoboto);
        color: var(--colorGray);
        font-size: 15px;
        padding-top: 45px;
    }
}

.hover-item {
    position: relative;
    background: var(--colorMint);

    &__move-block {
        display: block;
        position: relative;
        transition: all 0.6s ease;

        > img {
            object-fit: cover;
            position: absolute;
            width: 100%;
            height: 100%;
        }

        &::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: linear-gradient(90deg, rgb(252, 227, 138) 0%, rgb(243, 129, 129) 100%);
            opacity: 0;
            transition: all 0.6s;
        }
    }

    &__content {
        @mixin flex column, nowrap, center, center;

        font-size: 18px;
        font-family: var(--fontMontserrat);
        font-weight: bold;
        color: var(--colorWhite);
        position: absolute;
        width: 100%;
        height: 100%;
        opacity: 0;
        z-index: 10;
        transition: all 0.7s;

        > img {
            margin-bottom: 5px;
        }
    }

    &:hover,
    &:active,
    &:focus {
        cursor: pointer;

        .hover-item__move-block {
            transform: translateY(-10px) translateX(-10px);

            &::after {
                opacity: 0.9;
            }
        }

        .hover-item__content {
            opacity: 1;
        }
    }
}

.statistic-section {
    .flex-container {
        @mixin flex row, wrap, center, center;

        &__item {
            @mixin flex column, wrap, center, center;

            text-transform: uppercase;
            font-family: var(--fontMontserrat);
            color: var(--colorWhite);
            width: 70%;
            height: 130px;
            border-bottom: 1px solid #b5eae0;

            &:last-child {
                border: none;
            }

            > span {
                font-size: 50px;
                font-weight: bold;
            }

            @media (width >= 992px) {
                font-size: 11px;
                border-right: 1px solid #b5eae0;
                border-bottom: none;
                height: 185px;
                width: 20%;

                > span {
                    font-size: 50px;
                    font-weight: bold;
                }
            }

            @media (width >= 1200px) {
                font-size: 14px;
                height: 270px;

                > span {
                    font-size: 72px;
                }
            }
        }
    }
}

.info-card {
    @mixin flex row, nowrap, flex-start, flex-start;

    &__img {
        margin-right: 30px;
    }

    div {
        flex: 1;
    }

    &__title {
        margin-top: 0;
        font-size: 14px;
        font-family: var(--fontMontserrat);
        color: var(--colorDark);
        text-transform: uppercase;
    }

    &__text {
        font-size: 15px;
        font-family: var(--fontRoboto);
        color: var(--colorGray);
    }
}

.wrap-line {
    display: block;
    width: 100%;
    height: 1px;
    background: #e5e5e5;
    margin: 50px 0;
}

.card {
    max-width: 300px;
    margin: 0 auto;

    &-label {
        font-family: var(--fontMontserrat);
        font-size: 14px;
        color: var(--colorDark);
        text-align: center;
        padding: 30px 0;

        p {
            text-transform: uppercase;
        }

        span {
            font-family: var(--fontRoboto);
            font-size: 14px;
            font-weight: 300;
            font-style: italic;
            color: var(--colorGray);
            display: block;
        }
    }

    .btn-group > .btn {
        @media (width >= 768px) and  (width <= 992px) {
            width: 45px;
            height: 45px;
        }
    }

    .hover-item__move-block {
        position: relative;
        height: 390px;

        img {
            position: absolute;
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        @media (width >= 768px) {
            height: 320px;
        }

        @media (width >= 992px) {
            height: 390px;
        }

        @media (width >= 1200px) {
            height: 470px;
        }
    }
}

.brand-container {
    @mixin flex row, wrap, space-between, center;

    padding: 60px 10px;

    span {
        @mixin flex column, nowrap, center, center;

        width: 140px;
        height: 140px;

        @media (width >= 768px) and (width <= 992px) {
            width: 33.3%;
        }
    }

    @media (width <= 767px) {
        justify-content: center;
    }
}

.quote-carousel-block {
    margin-top: 110px;
}

.gallery-grid {
    @mixin flex row, nowrap, space-between, flex-start;

    &__section {
        width: 50%;
    }
}

.client-profile {
    @mixin flex row, nowrap, space-between, flex-start;

    max-width: 520px;
    min-height: 260px;
    margin: 0 auto;

    &__avatar {
        margin-right: 5%;
        flex: 1;
        width: 22%;
        max-width: 110px;
    }

    &__name {
        font-family: var(--fontMontserrat);
        font-size: 14px;
        color: var(--colorDark);
        text-transform: uppercase;
        margin-top: 5px;
    }

    &__subject {
        font-family: var(--fontRoboto);
        font-style: italic;
        font-weight: 300;
        margin-bottom: 20px;

        &::after {
            content: '';
            display: block;
            height: 4px;
            width: 60px;
            background: var(--colorRed);
            margin-top: 15px;
        }
    }

    &__description {
        font-family: var(--fontRoboto);
        font-size: 15px;
        font-weight: 500;
        color: var(--colorGray);
        line-height: 24px;
    }
}

.blog-thumbnail {
    display: block;
    max-width: 380px;
    margin: 0 auto;

    &__content {
        position: relative;
        height: 240px;

        img {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        @media (width >= 768px) {
            height: 140px;
        }

        @media (width >= 992px) {
            height: 200px;
        }

        @media (width >= 1200px) {
            height: 240px;
        }
    }

    &__date {
        @mixin flex column, nowrap, center, center;

        position: absolute;
        width: 65px;
        height: 70px;
        background: var(--colorMint);
        color: var(--colorWhite);
        font-family: var(--fontRoboto);
        font-weight: bold;
        font-style: italic;
        left: -10px;
        bottom: 10px;

        .number {
            font-size: 30px;
            font-family: var(--fontMontserrat);
            font-style: normal;
            margin-bottom: -8px;
        }
    }

    &__header {
        font-family: var(--fontMontserrat);
        color: var(--colorDark);
        font-size: 14px;
        text-transform: uppercase;
        margin: 15px 0;

        @media (width >= 768px) and (width < 992px) {
            height: 30px;
            overflow: hidden;
        }
    }

    &__text {
        font-family: var(--fontRoboto);
        color: var(--colorGray);
        font-size: 15px;
    }

    &__info {
        font-family: var(--fontRoboto);
        color: var(--colorGray);
        font-size: 15px;
        font-weight: 300;
        font-style: italic;
        padding: 15px 0;
        border-top: 1px solid #e5e5e5;
        display: block;

        span {
            margin-right: 20px;
        }

        i {
            color: var(--colorMint);
        }
    }

    &:hover {
        text-decoration: none;

        .blog-thumbnail__date {
            background: color(var(--colorMint) lightness(-15%));
        }
    }
}

.section-contacts {
    &__title {
        font-family: var(--fontMontserrat);
        font-size: 14px;
        color: var(--colorDark);
        margin-bottom: 40px;
    }
}

.contact-info {
    .contacts-logo {
        margin-bottom: 40px;
    }

    &__text {
        font-family: var(--fontRoboto);
        color: var(--colorGray);
        font-size: 15px;
    }

    &__follow-number {
        display: block;
        margin: 40px 0 15px;
        color: var(--colorDark);
        font-family: var(--fontMontserrat);
        font-size: 14px;

        span {
            font-size: 18px;
            font-weight: bold;
        }
    }

    &__follow-block {
        border-top: 1px solid #e5e5e5;
        padding: 14px 0 40px;
        font-family: var(--fontRoboto);
        color: var(--colorGray);
        font-style: italic;
        font-weight: 300;

        span {
            @media (width <= 767px) {
                display: block;
                margin-bottom: 10px;
            }
        }

        a {
            color: var(--colorMint);
            padding: 0 8px;
            font-size: 17px;
        }
    }

    .input-group {
        max-width: 80%;
    }
}

.blog-item {
    @mixin flex nowrap, row, flex-start, flex-start;

    margin-bottom: 45px;

    &__media {
        margin-right: 20px;
    }

    &__info {
        flex: 1;
    }

    &__header {
        color: var(--colorDark);
        font-family: var(--fontMontserrat);
        font-size: 12px;
        line-height: 18px;
        text-transform: uppercase;
        margin: 20px 0 0;
        height: 36px;
        overflow: hidden;
    }

    &__date {
        font-size: 13px;
        font-family: var(--fontRoboto);
        color: var(--colorGray);
        font-style: italic;
        font-weight: 300;
    }
}

.instagram-grid {
    @mixin flex wrap, row, flex-start, flex-start;

    margin-left: -2px;

    &__item {
        width: 88px;
        height: 88px;
        margin: 2px;
        transition: all 0.7s;

        img {
            max-width: 100%;
            object-fit: cover;
        }

        &:hover {
            opacity: 0.7;
        }
    }
}

.section-do-work {
    .img-responsive {
        @media (width <= 992px) {
            margin: 0 auto 40px;
        }
    }
}

.card-about {
    max-width: 380px;
    margin: 100px auto;

    .hover-item__move-block {
        @media (width <= 1200px) {
            height: 250px;
        }
    }
}
