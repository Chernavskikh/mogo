.footer {
    border-top: 1px solid #e5e5e5;
    padding: 20px 0;
    text-align: center;
    font-size: 14px;

    &__link {
        color: var(--colorRed);

        &:hover {
            color: var(--colorRed);
        }
    }
}
